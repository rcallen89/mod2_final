<p>Merchant Employee Information</br>
  <%= current_user.merchants.first.name %></br>
  <%= current_user.merchants.first.address %></br>
  <%= current_user.merchants.first.city %></br>
  <%= current_user.merchants.first.state %></br>
  <%= current_user.merchants.first.zip %></br>
</p>

<p>Orders</p>
<%@orders.each do |order|%>
  <p>Order number: <%=link_to "#{order.id}", "/merchant/orders/#{order.id}" %><br>
    Total Items: <%=order.items_by_merchant(current_user.merchants.first.id)%><br>
    Grand Total: <%=number_to_currency(order.grandtotal_by_merchant(current_user.merchants.first.id))%><br>
    Date Ordered: <%=order.creation%><br></p>
<%end%>

<%= link_to "My Items", merchant_items_path %>
<%= link_to "New Bulk Discount", '/merchant/discounts/new' %>
